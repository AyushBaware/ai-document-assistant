{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@react-oauth/google": "^0.13.5",
    "@tailwindcss/vite": "^4.3.0",
    "axios": "^1.16.1",
    "framer-motion": "^12.38.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "react-icons": "^5.6.0",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.15.1",
    "remark-gfm": "^4.0.1",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.3.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "vite": "^8.0.12",
    "vite-plugin-pwa": "^1.3.0"
  },
  "overrides": {
  "//": "brace-expansion pinned to 5.0.8 to patch CVE GHSA-mh99-v99m-4gvg (DoS via unbounded expansion) in the vite-plugin-pwa -> workbox-build -> ejs -> jake -> filelist -> minimatch chain, which resolved an older vulnerable copy. Verified via `npm ls brace-expansion` that this is the only copy in the tree post-override. Remaining `npm audit` warning (react-router GHSA-qwww-vcr4-c8h2) is a CSRF bypass in React Router's unstable RSC/server-actions mode, not used anywhere in this app (plain client-side SPA routing) -- accepted as not exploitable, do not downgrade react-router-dom to 'fix' it.",
  "brace-expansion": "5.0.8"
}
}